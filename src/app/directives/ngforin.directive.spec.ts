import { NgforinDirective } from './ngforin.directive';

describe('NgforinDirective', () => {
  it('should create an instance', () => {
    const directive = new NgforinDirective();
    expect(directive).toBeTruthy();
  });
});
